
const add = (a,b) => a + b; // { return a + b; }

const substract = function (a, b) {
    return a - b;
}

const multiply = (a,b) => a * b;


module.exports = {
    sum: add,
    substract,
    multiply
}